<link rel="stylesheet" href="{{ asset('front/css/styleProjet.css') }}">
<title>Edit Project</title>


{% block body %}
<div class="container project-form-container">
    <div class="project-form-header">
        <h1>Edit Project</h1>
        <p class="text-muted">Update the project details below</p>
    </div>

    {{ include('projet/_form.html.twig', {'button_label': 'Update', 'form_title': 'Edit Project'}) }}

    <div class="form-actions mt-4">
        <a href="{{ path('app_projet_index') }}" class="btn btn-back">
            <i class="fas fa-arrow-left me-2"></i> Back to list
        </a>
    </div>
</div>
{% endblock %}