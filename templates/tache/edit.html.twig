<!DOCTYPE html>

<title>Edit Tache</title>

{% block body %}
    <h1>Edit Tache</h1>

    {{ include('tache/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path(projetId ? 'app_tache_by_project' : 'app_tache_index', {'id': projetId}) }}">back to list</a>

    {{ include('tache/_delete_form.html.twig') }}
{% endblock %}
