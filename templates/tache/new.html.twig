<!DOCTYPE html>

<title>New Tache</title>

{% block body %}
    <h1>Create new Tache</h1>

    {{ include('tache/_form.html.twig') }}

    <a href="{{ path(projetId ? 'app_tache_by_project' : 'app_tache_index', {'id': projetId}) }}">back to list</a>
{% endblock %}
