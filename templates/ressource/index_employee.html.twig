<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Liste des Ressources</title>
    <style>
        /* Styles pour le tableau des ressources */
        .table {
            width: 80%;
            margin: 20px auto;
            border-collapse: collapse;
            border: 1px solid #ddd;
        }

        .table th, .table td {
            padding: 10px;
            text-align: left;
            border-bottom: 1px solid #ddd;
        }

        .table th {
            background-color: #007bff;
            color: white;
        }

        /* Styles des boutons */
        .btn-reserver {
            background-color: #28a745;
            color: white;
            padding: 10px 20px;
            font-size: 16px;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            transition: background-color 0.3s ease;
        }

        .btn-reserver:hover {
            background-color: #218838;
        }
    </style>
</head>
<body>
    <h1>Liste des Ressources</h1>
    <table class="table">
        <thead>
            <tr>
                <th>Nom</th>
                <th>Type</th>
                <th>État</th>
                <th>Prix</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
            {% for ressource in ressources %}
                <tr>
                    <td>{{ ressource.nom }}</td>
                    <td>{{ ressource.type }}</td>
                    <td>{{ ressource.etat }}</td>
                    <td>{{ ressource.prix }}</td>
                    <td>
                        <!-- Lien "Réserver" qui redirige vers la page de réservation -->
                        <a href="{{ path('app_reservation_new', {'ressourceId': ressource.id}) }}" class="btn-reserver">Réserver</a>
                    </td>
                </tr>
            {% else %}
                <tr>
                    <td colspan="5">Aucune ressource trouvée.</td>
                </tr>
            {% endfor %}
        </tbody>
    </table>
</body>
</html>
